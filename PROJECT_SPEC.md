# MR-SeqRec 项目技术规格书（v5 · 文献自查后重构 · 含评审修订与 Go/No-Go 判定）

> 状态：**文献自查已执行，核心创新点已重构；顶刊审稿人评审完成，修订清单与 Go/No-Go 判定已落定（§13）**
> 日期：2026-08-29
> 项目定位：中国大学生计算机设计大赛（2027届）+ 国际权威期刊（CCF-A 为目标，CCF-B/SCI-2 为现实锚点）
> 项目哲学：**解决真实生活里的一件小事**——养老健康平台上"商品图片/文字系统性缺失，推荐系统就失灵"。

---

## 0. 核心决策（含自查后修正）

| 决策项 | 结论 |
|---|---|
| 核心创新方向 | 原方案（dropout+重建+一致性）**已被抢先发表，作废**；重构为：**序列推荐 × (M)NAR 结构化缺失 × 不变学习 × 健康域**（见 §1.5；原"时间信号"已按数据信号闸门移除，§13.6-B） |
| 竞赛演示形态 | Web 演示原型（离线模型 + 可视化 Demo）；**国奖级演示设计蓝本已定（§14）：品牌化 6 模块，核心交互 = 缺失滑块对比 + 安全门控 + 关怀模式双模式** |
| 论文目标 | **现实锚点优先（已确认 2026-08-29）**：主投 CCF-B / SCI-2（KAIS/IPM/ESWA），实验强再冲 CCF-A |
| 主数据集 | Amazon Reviews 2023（Health/Beauty/Grocery）——**标准版无预计算特征，需自提或复用已处理版本**（见 §3） |
| 文本特征 | CLIP 文本编码器重编码（与图像同空间） |
| 时间策略 | 竞赛+论文并重、长周期；S0–S4 目标 2026 年底，2027 春投 |
| 分工 | 系统核心用户独立完成；演示/PPT/项目书后期交队友 |
| GPU | **已确认（2026-08-29）：Intel Arc 130T（16GB），无 CUDA**——torch 按 CPU 版安装；后续如需加速装 Intel Extension for PyTorch (XPU) 或借实验室 NVIDIA 服务器 |
| 增强模块 | 不确定性置信头：保留（作为不变学习的解释性组件） |
| **文献自查** | 已执行（2026-08-29），结果见 §12。**S0 必须先从深度 related-work 调研开始，架构不得在此前定稿** |
| **论文判定机制（Go/No-Go）** | 新增（2026-08-29）：S2 产出首个鲁棒曲线后，客观对照 SMD / DuAF-MAT；显著占优 → 继续冲论文；平手 → 回炉或转纯竞赛（见 §13.3，S2 硬性出口） |

---

## 1. 研究问题与定位

### 1.1 真实问题（论文的故事起点）

养老健康平台的商品信息由大量小商家上传，图片、文字经常**系统性缺失**。这种缺失**不是随机的**（MNAR）：高热度商品反而更常缺图、某些卖家类型整类缺文。当推荐模型只在"信息完整"的数据上训练，部署到真实不完整数据时性能骤降——直接影响老年人买到的是不是合适的健康产品。

### 1.2 问题定义

**Missingness-Invariant Multimodal Sequential Recommendation**：构造一个多模态序列推荐模型，使推荐表示对**任意模式（随机 MCAR / 结构化 MNAR）、任意比例、以及训练-部署间缺失分布漂移**均保持稳定（不变）。

### 1.3 已有工作与差异化（自查后修正）

| 方法 | 年份/venue | 做了什么 | 与本项目差异（S0 深挖后核实） |
|---|---|---|---|
| LRMM | EMNLP'18 | m-drop 模态 dropout + 序列自编码器重建 | 评分预测（非 top-k 序列）；CNN/LSTM 特征；识别块状缺失但无 MNAR 建模；无不变学习；无代码 |
| M3Rec | IEEE'22 | 微视频掩码模态预测 | 视频内容推荐，非序列商品 |
| M3SRec | CIKM'23 | 多模态 MoE + 掩码预训练 | 解决稀疏，不解决缺失鲁棒；代码公开 |
| UniSRec | KDD'22 | 文本通用序列表示 | 跨域迁移，非缺失鲁棒；代码公开 |
| MILK | SIGIR'24 | 跨环境不变学习（IRM）+ cyclic modality mixup | **新物品/冷启动推荐（非序列）**；训练/测试缺失按随机 MCAR 注入；无显式 MNAR 建模；无时间/健康信号；代码公开 |
| I3-MRec | ACM MM'25 | IRM 不变学习（每模态=环境）+ 信息瓶颈 + 缺失感知融合 | **LightGCN 图协同过滤（非序列）**；仅 MCAR 随机掩码（零向量）；无 MNAR/时间/健康；FTMT/MTMT 协议；代码公开 |
| SMD | **CIKM 2026**（已接收） | 逐**序列**模态 dropout（同序列共享掩码）+ retention 评估 + 可选互预测重建 | 序列全覆盖，但属 dropout 数据增强（生成式），**无 MNAR 机制建模、无显式不变学习目标、无时间/健康信号**；代码公开、数据需自备 |
| **Meta-Modal Agent（MMA）** | arXiv 2026-05 | LLM 重排器把缺失模态当**序列证据路由**：动态决定查哪个模态，**把失败的/Null 查询当有意义观测** | 检索后处理 + LLM 推理，非表示学习；无显式 MNAR 统计建模、无缺失不变表示、无健康域；**与"缺失即信息"叙事相邻，必须在 intro 划界：证据路由 ≠ MNAR 统计建模 ≠ 缺失不变表示** |
| VLM2Rec | arXiv 2026-03 | 解决 VLM 嵌入器**模态坍缩**：弱模态罚项对比 + 跨模态拓扑正则 | 面向特征嵌入质量，非缺失鲁棒；非直接竞争者，但审稿人可能问边界 |
| **DuAF-MAT（最接近竞争者）** | AAAI 2026 | **序列**多模态：DuAF 时间感知门控 + MAT-MoE 对抗生成补全缺失模态 + MSCL 对比 | 已占"序列×时间门控×模态失衡补全"；但缺失=随机 dropout 仿真（**把缺失当噪声"补"**）、**无 MNAR/信息性缺失建模、无不变学习、电商场景无健康域**；无公开代码 |
| **Emory'26（Two-Sided SSM）** | Emory ETD 2026 | **序列**推荐中 **modality-MNAR 显式建模**：把模态缺失当信息性信号 + endogeneity 加权消息传递 + 双端时序状态演化；6 Amazon 类目 Recall@20 超 BSARec 14.8–18.8% | **占据"序列 × 模态缺失 MNAR 建模"（原贡献 1 立足点，四审发现）**；无不变学习、无健康域；图消息传递 + state-space ≠ 序列注意力 + 缺失不变表示；**需核对其是否有会议/期刊版** |
| TKDE'26 | TKDE 2026 | 共购图特征传播补全缺失模态（training-free、模型无关） | 非序列（VBPR/LightGCN-M 等图 CF）；MCAR 遮蔽；无不变学习/无 MNAR/无时间健康；代码公开 |
| PMMRec / ColdFusionX | 2023+ | 单模态迁移 / 随机掩码冷启动 CTR | 迁移/冷启动，非序列缺失鲁棒 |

> 深挖结论（2026-08-29 四审独立重检后更新）：**"序列"与"时间门控"维度已被 SMD(CIKM'26)、DuAF-MAT(AAAI'26) 占据**；**"模态缺失 MNAR 建模 × 序列推荐"已被 Emory'26（Two-Sided SSM）占据**——原贡献 1 的"首次"声明作废；**"缺失即信息"叙事另有旁路竞争者 MMA（LLM 证据路由，Null=信息）**；**唯一仍空的两点：① 缺失 × 不变学习（IRM 风格）× 序列的组合（独立核查：无任何工作三者合一）② 健康/时间域**。→ 立身之本移锚为 **缺失不变学习（序列）× 健康域** 的组合，落在序列设定里；MNAR 建模降为"环境构造手段"而非 novelty 声明。

### 1.4 核心贡献（S0 深挖后精化）

**立身之本（2026-08-29 四审移锚；数据信号闸门后微调）：序列推荐中的缺失不变表示 × 健康域。** 原"MNAR 显式建模"的首次声明已被 Emory'26 占据（见 §1.3），故降为支撑手段：缺失模式仍被当作"信息"来构造环境（而非噪声），但**新颖性锚点转移到"对任意缺失环境保持不变的序列表示（缺失不变学习）× 健康域应用场景"**——这两点的组合经独立核查仍无人占据。（原"健康域复购周期信号"已按 2026-08-29 数据信号闸门移除：Amazon Health 无复购记录，见 §13.6-B。）**免疫范围精确化（2026-08-29 风险复核）："免疫"不是对缺失的全部免疫，而是对缺失中"伪相关/混淆"的部分免疫（如流行度代理——避免误杀协同过滤信号），对缺失中"信息性"的部分保持敏感（如健康属性相关）；即免疫对象是"环境"（缺失配置），保留对象是"有效信号"。若此界限不写清，"缺失即信息"与"缺失不变"会自相矛盾，审稿人必攻。**

1. **序列推荐中的信息性缺失（MNAR）显式建模**：建模缺失概率与流行度/类目等可观测因素的关系（缺失倾向估计）——**Emory'26 已占"建模"本身，本项目定位为"用缺失模式构造不变学习的环境"（支撑手段，非 novelty）**；
2. **缺失不变学习（novelty 锚点 1，四审独立确认无人占据）**：把缺失模式作为混淆因素/环境变量，用不变学习原则（IRM 风格）训练序列推荐表示，使其对任意缺失模式稳定——这是与 DuAF-MAT"对抗补全"、Emory"endogeneity 加权"**定位相反**的路径（不补全、不加权，学不变）；
3. **缺失分布漂移下的鲁棒泛化评估协议**：训练一种缺失分布、测试 OOD（更高缺失率、未见 MNAR 模式），配套鲁棒性汇总指标（retention / AUC 曲线）；
4. ~~**健康域时间信号（novelty 锚点 2）**：时间间隔编码（慢性病周期性复购/用药间隔代理）~~ —— **已移除（2026-08-29 数据信号闸门）**：Amazon Health reviews 结构上无复购周期（实测 (user,item) max count=1，每用户每商品至多一条交互），慢性病复购/用药间隔代理在该数据集上不可验证，贡献作废（执行记录见 §13.6-B）；有效贡献收敛为 1/2/3/5；
5. **不确定性/证据加权融合**：缺失下的置信估计 → 解释性与安全门控（竞赛差异化）。

> **可证伪研究主张（2026-08-29 评审补充）**：显式建模 MNAR 信息性缺失 + 缺失不变学习，在序列多模态推荐的缺失鲁棒性上**显著优于**把缺失当噪声随机丢弃（SMD）或对抗补全（DuAF-MAT），且全模态（E1）不降。对应实验 E2/E3/E4（详见 §13.2）。

### 1.5 诚实定位（写进论文 intro）

> 单点技术（dropout、重建、不变学习 IRM、MNAR 建模）均有先例。S0 深挖 + 四审独立重检后的诚实结论：
> - 已有工作把缺失当**噪声**去"躲"（SMD 随机 dropout）或去"补"（DuAF-MAT 对抗生成、TKDE 图补全）或去"加权"（Emory'26 endogeneity）；
> - **模态缺失 MNAR 的信息性建模在序列推荐中已被 Emory'26 占据**；
> - **没有任何工作把缺失不变学习（IRM 风格）放进序列推荐**（独立核查：missingness+IRM+sequential 三者无合一）；
> - **健康/适老化领域 + 缺失鲁棒的组合无人占据**。
>
> 因此本工作的增量主张是：**序列推荐中的缺失不变表示 × 健康域**（MNAR 建模降为环境构造手段）。intro 需逐条引用 SMD/DuAF-MAT/MILK/I3-MRec/LRMM/**Emory'26**/MMA 并说明差异（§1.3 表），尤其要与 DuAF-MAT（AAAI'26）**显式划界：对抗补全 ≠ 缺失不变学习 ≠ 证据路由 ≠ MNAR 建模**。

---

## 2. 模型架构（MR-SeqRec v4）

### 2.1 输入与特征

| 模态 | 来源 | 维度 | 说明 |
|---|---|---|---|
| ID 嵌入 e_i | 可学习 | d=64–128 | 协同信号 |
| 图像特征 f_i^v | CLIP ViT-B/32（**自提取**，见 §3.2） | 512 | |
| 文本特征 f_i^t | CLIP 文本编码器重编码 | 512 | 与图像同空间 |

投影：`v_i = LayerNorm(W_v f_i^v)`，`t_i = LayerNorm(W_t f_i^t)` → d 维。

### 2.2 缺失感知融合

- 掩码 `m_i∈{0,1}²`；缺失模态 → 可学习缺失嵌入 μ_v/μ_t；
- 门控 `g_v,g_t = σ(W_g[e_i;v_i;t_i;m_i])`（显式接收掩码）；
- 证据加权：置信头 `κ_v,κ_t`（缺失时趋低）；
- 物品表示：`a_i = e_i + κ_v·g_v⊙v_i + κ_t·g_t⊙t_i`；
- 双模态全缺：退化为 `e_i + κ·g⊙μ`（优雅退化）。

### 2.3 序列编码

- SASRec 单向自注意力；L≤50；
- ~~时间间隔编码~~ —— **已移除（2026-08-29 数据信号闸门）**：Amazon Health 无复购周期，时间信号贡献作废（见 §13.6-B）。

### 2.4 缺失不变学习目标（核心，替换原"一致性+重建"的朴素组合）

把缺失模式视为环境 `e∈E`（环境=不同的缺失配置）。在每环境内优化：

```
L = L_BCE(推荐) + λ1·L_recon(重建, 能补) + λ2·L_inv(不变, 不依赖)
```

- **L_recon**：序列上下文 + 剩余模态重建缺失模态（投影空间 MSE + InfoNCE）；
- **L_inv（核心新意）**：跨环境表示的**不变正则**——对同一序列在不同缺失环境下的表示，约束对齐（IRM 风格 / 环境间一致性 / 表征不变），并对 MNAR 环境加权（缺失概率越相关，权重越高）；
- > ⚠️ **待定式（2026-08-29 二审问题 2）**：L_inv 以上三种表述并存，**S2 前必须锁定为单一精确目标函数**（IRM 梯度惩罚 / 跨环境表示对齐 / 环境间距离之一），否则无法实现且进不了论文。
- > **选型优先级（2026-08-29 风险复核）**：IRM 梯度惩罚已知失败模式多（Rosenfeld et al. ICLR'21：常不优于 ERM、非线性下可灾难性失败、对采样敏感、所需环境数随伪特征维度线性增长），**优先选"跨环境表示对齐 / 环境间距离"等更稳形式，"IRM 梯度惩罚"降为备选**；同时须验证"环境=缺失配置"的多样性（环境不足时不变学习学不到东西）。
- > **反平凡性声明（2026-08-29 三审）**：L_inv 必须在**训练未见过的缺失模式**（极端缺失率 / 未见 MNAR 类型 / 更大分布漂移）下提供**超出匹配强度 dropout（SMD 式增强）**的鲁棒——否则与 SMD 无本质差异。此声明由 E4 + E5 + E10（匹配强度对照）联合实证，是对 SMD 划界的实证根基。
- 训练时缺失采样：随机 MCAR + 结构化 MNAR 双通道。

### 2.5 推理

重建/不变项不参与，仅门控+置信计算 → 推理成本与基线持平（E12 实测支撑）。

---

## 3. 数据方案

### 3.1 数据集

Amazon Reviews 2023（Health/Beauty/Grocery）。**注意：标准 HF 版只有原始图片 URL 与文本，无预计算特征。**

### 3.2 特征获取（S0 优先验证，三条路线并行评估）

1. **复用已处理数据**：SMD / I3-MRec / MILK 等 2024–2026 工作已发布处理过的 Amazon 数据（含 CLIP 特征），优先复用省时省力；
2. **官方原始版 + 自下载图片**：Amazon CDN URL 大量失效风险高，需实测可下载率；
3. **老版数据集特征**：McAuley 老版 VGG 特征（与 CLIP 不对齐，降级方案）。

### 3.3 预处理

- 5/10-core 过滤；时间排序；L≤50；leave-one-out 划分（固定种子、发布）；
- 受控实验子集：双模态齐全物品，自行施加受控缺失；
- **缺失模拟双通道**：比例型（每模态以 ρ 概率缺失）+ 覆盖型（ρ 比例物品整体缺某模态）+ **MNAR 型**（缺失与流行度/类目相关）。

---

## 4. 基线与评估

### 4.1 基线（自查后修正名单）

- 纯 ID 序列：SASRec、BERT4Rec、GRU4Rec；
- 多模态序列：M3SRec、UniSRec、MM-SASRec；
- **缺失鲁棒竞争者（必须对比）**：**SMD、I3-MRec、MILK、LRMM**、DuAF-MAT；
- 朴素多模态 SASRec（零填充）作为"不建模缺失"对照；
- **Missingness-aware 版基线**（同样 dropout 增强训练）隔离架构收益 vs 增强收益。**⚠️ 对照强度要求（2026-08-29 三审）：该基线的 dropout 增强必须与我们方法同采样 schedule、同强度（匹配强度对照），否则"架构收益 vs 增强收益"不干净；此对照即"L_inv ≠ dropout"的反平凡性证据（E10 + E5 + E4 联合实证）。**

### 4.2 评估协议

- 主协议：leave-one-out + 100 负采样；补充：验证集子集全排序；
- **鲁棒性汇总指标**：相对保留率 `perf@ρ / perf@0`（对齐 SMD 的 retention）、鲁棒曲线下面积；
- 指标：Recall@10/20、NDCG@10/20；
- **MNAR 与 OOD 缺失评估**（E3/E4）：训练一种缺失分布、测试多种未见配置。

### 4.3 统计严谨性

3–5 种子 mean±std + 显著性检验；固定负采样/划分种子；开源。

---

## 5. 实验矩阵

| # | 实验 | 目的 |
|---|---|---|
| E1 | 全模态主结果 | 完整数据下不弱于 SOTA |
| E2 | MCAR 缺失鲁棒曲线 | 核心结果：保留率更高 |
| E3 | **MNAR 结构化缺失**（流行度/类目相关） | 差异化核心：贴近真实平台 |
| E4 | **缺失分布漂移 OOD**（训练/测试分布解耦） | 不变学习价值的直接证据 |
| E5 | 消融 | 去不变正则/去重建/去缺失嵌入/去门控/仅ID/去ID |
| E6 | 冷启动/部分模态/双模态全缺 | 边界场景 |
| E7 | 超参敏感性 | λ、r_max |
| E8 | 不确定性分析 | 置信 vs 缺失关联、校准图 |
| E9 | 案例展示 | 适老化产品示例（类目、季节） |
| E10 | Missingness-aware 基线公平对比 | 隔离架构收益 |
| E11 | 鲁棒性汇总指标 | 保留率/AUC 曲线 |
| E12 | 参数/延迟对比 | 支撑边缘友好声明 |
| E13 | 重建质量分析 | 防"重建 trivial"质疑 |

---

## 6. 工程与复现

（沿用 v3：PyTorch、特征离线、固定种子、开源、分阶段渐进式。**2026-08-29 更新：代码只注释关键逻辑，不做逐行注释；工程质量要求极高——类型标注、模块化、配置化、含测试（tests/）、零错误。**结构见 §6 代码树。）

---

## 7. 开发路线图（自查后调整）

| 阶段 | 内容 | 产出 | 预计 |
|---|---|---|---|
| S0 | **related-work 深度调研（非完成不可）**：精读 SMD / MILK / I3-MRec / LRMM / TKDE'26 / PMMRec / DuAF-MAT，输出《竞争定位表》+ 可用已处理数据；**架构据此定稿** | 竞争定位报告 + 数据就绪 | 2–3 周 |
| S1 | 复现基线（SASRec、MM-SASRec）+ 评估管线 + retention 指标 + **数据信号检验（三审 3.2）——已执行（2026-08-29）**：Amazon Health 实测无复购（(user,item) max count=1）→ 时间信号贡献移除 | 可靠评估管道 + 健康叙事可行性结论（已出：复购信号不可用，时间贡献已移除） | 2–3 周 |
| S2 | 实现 MR-SeqRec v5（MNAR 信息性缺失建模 + 缺失不变学习 + 重建 + 不确定性） | 首个 MCAR/MNAR 鲁棒曲线 → **触发 Go/No-Go 判定（§13.3）**；**Go 后立即发 arXiv 预印本占位（四审，原 S5 提前）** | 3–4 周 |
| S3 | 补齐全部基线与三数据集实验（含 SMD/MILK/I3-MRec 复现或获取其结果） | 完整结果表 | 3 周 |
| S4 | 超参 + 消融 + MNAR/OOD + 统计检验 | 论文级结果 | 2 周 |
| S5 | Claims 表核对 + 论文正式稿（投稿） | 可投递论文 | 3–4 周 |
| S6 | 竞赛 Web 演示（**按 §14 蓝本**：品牌 + 6 模块——时间线场景/缺失滑块对比/安全门控/关怀模式/retention 总览；**调用真实训练模型输出，禁止 mock**） | 可演示系统（国奖级呈现目标） | 并行 |

> 校级约 2026 年底–2027 年初。**S0–S4 目标 2026 年底完成**；投稿窗口 2027 春。若 S0 调研发现交叉缝隙仍被占据，则及时转向现实锚点（CCF-B/SCI-2）并调整方法侧重。

---

## 8. 风险速览（自查后补充）

| 风险 | 对策 |
|---|---|
| **核心交叉缝隙仍被占据** | S0 调研为非完成不可；发现即 pivot |
| **领域更新极快（2026 年 SMD/DuAF-MAT/Emory/MMA 密集出现，四审实证：数小时内缝隙被占）** | 贡献点锚定"缺失不变学习×健康"（独立核查仍空）；**arXiv 预印本提前至 S2 末（Go 后立即发）**；S0–S2 紧凑推进 |
| **SMD/MILK/I3-MRec 复现成本** | 优先复用其官方代码/处理数据；无法复现则引用其报告结果做对比 |
| **Amazon 图片 URL 失效** | 复用已处理数据 > 自下载（实测可下载率） |
| **健康复购信号可能不存在（Amazon Health 无周期性复购）** —— **已确认（2026-08-29 数据信号闸门）：风险成真**，Amazon Health 结构上无复购（每用户每商品至多 1 条交互） | **已执行**：时间信号贡献移除、健康叙事弱化为"健康类目应用域"（§13.6-B） |
| 基线源码质量不一 | MMRec / 官方源码优先 |
| AWS 下载慢 | 代理/镜像/HF 镜像 |
| **Intel Arc 130T 无 CUDA（已确认）** | S1 用 CPU torch 跑通（基线规模足够）；真实实验若过慢，装 IPEX (XPU) 加速或借实验室 NVIDIA 服务器 |
| 新手学习曲线 | 分阶段渐进、每阶段有可运行检查点；**代码工程质量优先（类型标注/模块化/测试），仅关键部分注释** |

---

## 9. 深度漏洞自查（18 条保留）

> 自查后结论：18 条**作为工程与实验严谨性要求全部仍然成立**（基线公平、统计显著性、可复现、ID 兜底消融、辅助任务伤主任务、数据获取、边缘实测、伦理等）。**但"新颖性定位"一条的威胁被证实远大于原判断**——原方案核心已被 2024–2026 工作覆盖（见 §12）。原 18 条细节见 git 历史 v3 版本，此处不再重复罗列，保留其作为实验设计与写作清单。

---

## 10. 论文写作与投稿策略

### 10.1 Claims 表（每条贡献↔实验证据）

| Claim | 支撑实验 |
|---|---|
| 缺失不变学习优于 SMD（序列+随机缺失） | E2 + E3 + E4 + E10 |
| 优于 MILK/I3-MRec（非序列不变学习） | E1 + E2（序列设定）+ E5 |
| MNAR 建模带来额外鲁棒 | E3 + E5（去 MNAR） |
| 不确定感知融合有效 | E8 + E5 |
| 全模态下不弱于 SOTA | E1 |
| 推理零额外开销 | E12 |

### 10.2 投稿路线（自查后修正为现实锚点优先）

- **现实锚点**：CCF-B / SCI-2（KAIS / IPM / ESWA）或高水平会议（SIGIR/CIKM/AAAI 短文）——保证可发表；
- **冲刺目标**：若 S0–S4 实验强、缝隙未被占，再投 CCF-A（TKDE / TOIS / Information Fusion）；
- 节奏：**arXiv 预印提前至 S2 末（Go/No-Go Go 后立即发布，四审调整）** → S5 正式稿 → 主投；投稿周期长，2027 春投出。
- **现实预期（2026-08-29 评审补充）**：即使结果扎实，第一投被拒是常态，通常 1–2 轮重投、周期 6–12 个月；CCF-B/SCI-2 现实可达，CCF-A 属冲刺项（需深度 + 结果 + 写作接近满分）。

---

## 11. 待办与下一步

- [x] 文献自查（2026-08-29）——**发现原方案核心已被抢先发表，作废**（§12）
- [x] 重构创新点：序列 × (M)NAR × 不变学习 × 健康时间信号（§1.4–1.5）
- [x] 修正基线名单 / 数据特征方案 / 投稿策略（§3/§4/§10）
- [x] **用户决策（2026-08-29）**：确认重构方向 + **现实锚点优先**（CCF-B/SCI-2 主投，CCF-A 冲刺）
- [x] **S0 深挖（第一批，6 篇）完成**：交叉缝隙核实——"MNAR×不变学习×健康域"在序列推荐中无人占据；发现最近竞争者 **DuAF-MAT(AAAI'26)**；SMD/MILK/I3-MRec/LRMM/TKDE 细节已核实（§1.3/§12.3）
- [x] **评审修订清单落定（2026-08-29）**：问题七 9 条 + 补充 6 条 + Go/No-Go 判定机制全部写入 §13
- [x] **二轮评审修订（2026-08-29）**：新增竞争者 MMA/VLM2Rec（§1.3/§12.3）+ 新问题清单 8 条（含复核结论）写入 §13.5；§12.3 MNAR 表述修正
- [x] **三审补充落定（2026-08-29）**：反平凡性要求（§4.1 匹配强度对照 + §2.4 声明）+ 数据信号检验（§7 S1/§8 风险）写入 §13.6
- [x] **四审独立重检落定（2026-08-29）**：Emory'26 占据"模态缺失 MNAR×序列"→ 立身之本移锚（§1.3–§1.5/§12.3）+ 预印本提前 S2 末（§7/§8/§10.2）写入 §13.7
- [x] **演示设计蓝本落定（2026-08-29）**：§14（品牌化 6 模块 + 安全门控 + 关怀模式双模式 + 自检评分 + 工程红线），S6 据此实现
- [x] **S1 骨架完成（2026-08-29）**：包结构 + 数据管线（k-core/leave-one-out/负采样）+ SASRec + 评估（Recall/NDCG/retention）+ 信号检验（同商品复购周期判定）+ 19 测试全绿 + synthetic 冒烟通过
- [x] **真实数据信号检验已执行（2026-08-29）**：Amazon Health（25.27M 交互/12.5M 用户/797K 商品）`signal-check` 结论 **SIGNAL_WEAK**（repurchase pairs=0，结构上无复购周期）→ **时间信号贡献（原贡献4）移除、健康叙事弱化**（§13.6-B）；**真实数据 SASRec 基线训练待跑**
- [ ] 用户补读 **5 篇** PDF 原文核实细节（SMD / MILK / I3-MRec / DuAF-MAT / **Emory'26**）
- [ ] **核实 Emory'26 是否有会议/期刊版**（影响划界写法与预印本紧迫度）
- [x] 数据获取与验证（Amazon-2023 Health_and_Household）：下载 + 转换 `data/amazon/Health.txt`（25.27M 交互）完成（2026-08-29，见 §13.6-B）
- [ ] 特征复用/提取（S0 路线：SMD/I3-MRec 已处理 CLIP 特征优先，见 §3.2）
- [ ] **S2 结束触发 Go/No-Go 判定**（§13.3，S3 开始前的硬性闸门）

---

## 12. 文献自查结论（2026-08-29，本版最重要的修订依据）

### 12.1 我的原始论断错误清单
- FREEDOM(KDD'23)=缺失模态 ❌ → 实为 ACM MM'23 图冻结/去噪
- M2TRec(SIGIR'25)=掩码模态迁移 ❌ → 实为 RecSys'22 元数据多任务（该论文不存在）
- M3REC(Kang,SIGIR'22) ⚠️ → 实为 M3Rec(IEEE'22,人大)，有掩码模态预测
- MMSRec(AAAI'23) ❌ → 查无此文（项目书基线名单有误）
- modality dropout 首现 NMT ❌ → 实为 LRMM(EMNLP'18,推荐领域)
- Prompt-MF(ECML'23) ❌ → 查无此文
- Amazon-2023 自带 CLIP 特征 ❌ → 标准版只有原始 URL+文本

### 12.2 核心发现：原方案已被抢先
- **LRMM(EMNLP'18)**：modality dropout + 序列自编码器重建（方案雏形）
- **SMD(2026)**：序列模态 dropout + retention 评估 + 可选重建（几乎就是原项目书方案）
- **I3-MRec(ACM MM'25)**：IRM 不变学习 + 缺失感知融合（覆盖"一致性/不变性"）
- **MILK(SIGIR'24)**：跨缺失环境不变学习 + 训练/测试分布漂移（覆盖"跨分布鲁棒"）
- **TKDE(2026)**：顶刊正式化"多模态推荐中缺失模态"问题

### 12.3 幸存的开放缝隙（S0 深挖后精化，2026-08-29）

六个精读代理核实后的结论：

- **序列**维度：已被 **SMD(2026)**、**DuAF-MAT(AAAI'26)** 占据；
- **时间门控**维度：已被 **DuAF-MAT** 部分占据（电商时间间隔门控）；
- **不变学习**维度：已被 **MILK(SIGIR'24)/I3-MRec(ACM MM'25)** 占据，但**都在非序列设定**（新物品/图 CF）；
- **MNAR/信息性缺失建模**：**模态缺失（modality）的 MNAR 在序列推荐中已被 Emory'26 占据**（Two-Sided SSM，endogeneity 加权）；仅剩"与不变学习结合"未占（临床 MMNAR 工作仅见 EMNLP'25/ACL'26 findings）；**推荐领域 MNAR 主流指反馈缺失去偏（IPS/DR/PU，2018 起成熟，2026 仍有新作）——属另一条线，intro 必须明确区分**；
- **健康/适老化领域**：无人占据。

→ 本项目立身之本（四审移锚；数据信号闸门后微调）= **缺失不变学习 × 健康域**，落在序列设定；MNAR 建模降为环境构造手段。（"健康时间信号"已因 Amazon 无复购周期移除，见 §13.6-B。）**DuAF-MAT(AAAI'26) 是最接近的方法竞争者；Emory'26 占据"模态缺失 MNAR 建模"；MMA(2026-05) 占据"缺失即信息"叙事**——写作中必须与三家划界。

**诚实警告**：本领域 2026 年论文密集出现，缝隙是"动态的"——需要在 S0–S2 紧凑推进并尽早发 arXiv 预印本占位。

### 12.4 对 18 条自查的定性
工程/实验严谨性类（公平对比、统计、复现、消融、伦理等）**全部成立**；"新颖性定位"一条**严重低估了威胁**。

### 12.5 深挖方法学说明（可复现性）
本次深挖基于 arXiv 元数据、Semantic Scholar 摘要、中文精读笔记等多源交叉验证（每个 agent 至少 2 个独立来源）。**注意：本环境 WebFetch 大量域名被拦截，多数报告未能直接读原文全文**，个别细粒度数值（如缺失比例、超参）标注"未能核实"。S0 正式调研需由用户在可用网络下补读 SMD/MILK/I3-MRec/DuAF-MAT 四篇 PDF 原文核实。

---

## 13. 评审修订清单与论文发表判定（2026-08-29 落定）

> 背景：以"顶刊权威审稿人 + 竞赛资深评委 + 推荐领域专家"身份，**仅基于项目书.docx** 完成的客观评审。
> 总体结论（客观评分）：选题 A-、技术方案 C、新颖性 D、实验设计 C+；**作为论文"原样不可发表"（即使 CCF-B 也不行）**；竞赛"省赛一等奖可期、国赛二等奖左右"。
> 本节把评审的《问题七 修改清单》、评审后补充的判定要求、以及 Go/No-Go 机制全部落成可执行条目，作为项目下一步的工作清单。

### 13.1 问题七：9 条修改清单（评审原文 → 落定状态）

| # | 修改项 | 性质 | 落定状态 |
|---|---|---|---|
| 1 | 新增 related work，与 LRMM/M3Rec/SMD/MILK/I3-MRec/DuAF-MAT 逐条划界 | 防拒 | 已在 §1.3/§1.5；投前再核查最新文献 |
| 2 | 核心创新点重构（原 masked-reconstruction 已被抢先发表） | **贡献（论文真正的科研工作本身）** | 已重构为 §1.4–1.5 |
| 3 | 修正基线名单 + 公平对比协议（含 Missingness-aware 版基线） | 防拒 + 证据 | 已修正为 §4.1–4.2 |
| 4 | 缺失模拟扩展：覆盖型 + MNAR 型 + 分布漂移 | 证据 | 已在 §3.3 / §5（E3/E4）；S2 实现阶段落实 |
| 5 | 双模态全缺的边界与问题定义明确 | 防拒 | 已在 §2.2 优雅退化 + §5（E6） |
| 6 | 评估协议细节 + 多种子显著性检验 | 防拒 | 已在 §4.2/§4.3 |
| 7 | 数据集可行性验证（Amazon-2023 无预计算特征） | 防拒 | 已在 §3.2 三路线 + §11 待办 |
| 8 | 演示形态 + 适老化叙事 | 竞赛 | 已在 §7（S6） |
| 9 | 英文标题连字符修正 | 锦上添花 | 写作时落实 |

### 13.2 评审后额外补充项（2026-08-29 新增）

1. **可证伪的研究主张（hypothesis）**：一句话、可被实验推翻的声明——见 §1.4 末尾（对应 E2/E3/E4）。
2. **正证据要求（决定性）**：论文被接受靠"方法真的 work"，不是"没有明显错误"。硬要求：
   - 效应量显著且可感知（不是勉强高 0.5 个点）；
   - 三数据集结果方向一致；
   - 多种子 mean±std + 显著性检验；
   - 全模态（E1）不降。
3. **复现即底线**：代码 + 数据管线 + 配置 + 种子真实可跑（§6），评审可复现是防拒底线。
4. **深度要求（"第一个占坑"≠"能发"）**：顶刊要的是在空位里挖出深度，而非只证明"我们是第一个组合"。论文必须正面回答审稿人追问：
   - 为什么序列设定里的不变学习是必要的？与 MILK/I3-MRec（非序列）的差异是否带来新机制？
   - MNAR 建模的假设是什么、可识别吗？（缺失倾向与流行度/类目的关系模型）
   - ~~健康时间信号为什么不是可有可无的包装？~~ —— **已随贡献移除而解决（2026-08-29 数据信号闸门）**
5. **投稿现实预期**：第一投被拒是常态，通常 1–2 轮重投、周期 6–12 个月（§10.2）。
6. **arXiv 预印本占位**：S5 末尽早发布，抢"缺失即信息 × 序列"表述（2026 年领域更新极快）。

### 13.3 Go/No-Go 判定机制（核心，最高优先级）

**S2 检查点**（首个 MCAR/MNAR 鲁棒曲线产出时触发，即 §7 S2 结束、S3 开始前）：

- **对照对象**：SMD（序列+随机缺失）与 DuAF-MAT（AAAI'26，最接近竞争者），用同一批已处理数据、同一评估协议、Missingness-aware 公平基线（§4.1）；
- **Go**：显著占优（三数据集一致 + 全模态 E1 不降）→ 继续 S3–S5 冲论文；
- **No-Go**：平手或更差 → 说明"缺失即信息"主张未被实证支持，回炉改方法；改不动则**放弃论文野心、转纯竞赛**（竞赛不依赖此新颖性）。
- 判定依据（实验日志 + 结果表）作为投稿前的硬性闸门记录。

### 13.4 一句话结论

> 评审清单把"必被拒"变成"可投稿"，是**必要条件**；论文能否发表由 **13.2 正证据 + 13.3 判定结果**决定。**论文是"实验做出来"的，不是"改出来"的。**

### 13.5 二轮评审新增问题清单（2026-08-29，已逐条复核）

> 来自二审（2026-08-29）新发现问题，每条标注**复核结论**（对照规格书原文 + 当日文献核查）。

| # | 问题 | 复核结论 | 证据/说明 | 处理动作 |
|---|---|---|---|---|
| 1 | §1.3 竞争表缺 MMA / VLM2Rec | **完全证实** | 当日检索确认：MMA(arXiv 2605.25007, LLM 证据路由, Null=信息)、VLM2Rec(arXiv 2603, 模态坍缩)；原表未收录 | 已补入 §1.3；intro 与 MMA 划界 |
| 2 | L_inv 未定式 | **完全证实** | §2.4 仅"IRM 风格/环境间一致性/表征不变"三种说法，无单一目标函数 | **S2 前必须定式**（已标注 §2.4） |
| 3 | MNAR 倾向模型可识别性未论证 | **完全证实** | §1.4 只写"缺失概率与流行度/类目关系"，无参数化、无识别条件；倾向与推荐信号 confound | S1/S2 补充：参数化形式 + 识别条件 + 解耦消融 |
| 4 | "MNAR"术语负载 | **证实（含规格书自身表述需修正）** | 推荐界 MNAR 主流指反馈缺失去偏（IPS/DR/PU，2018 起成熟，2026 仍有新作）；§12.3 原"推荐领域无人占据"不精确 | 已修正 §12.3；intro 区分"模态缺失 MNAR ≠ 反馈缺失 MNAR" |
| 5 | 自然缺失未进评估 | **完全证实** | §3.3/§5 仅受控模拟三通道，无真实自然缺失评估 | 新增 E14：数据自带自然缺失分布评估 |
| 6 | 基线公平性（MILK/I3-MRec 客场、DuAF-MAT 无码） | **部分存在** | §8 已覆盖"SMD/MILK/I3-MRec 复现成本"，但未覆盖"客场公平性"与"DuAF-MAT 无码自实现" | §4.1/§8 补充：自实现版本公开、客场声明、引用原报告数字需注明不可比 |
| 7 | 六组件堆叠 | **完全证实** | v5 原含门控+证据+重建+不变+时间+MNAR 六件；§7 S2 无分步计划 | S2 改最小核（MNAR+不变）起步，逐件加，每件消融；**时间组件已移除（§13.6-B）** |
| 8 | 健康时间信号是代理 | **部分存在** | §1.4 已诚实标注"代理"，但缺 E13 直接证据与代理合理性论证 | **已解决（2026-08-29 数据信号闸门）**：Amazon 无复购周期，时间信号贡献整体移除，代理边界问题随之消失 |

**复核结论汇总**：5 条完全证实（1/2/3/5/7），2 条部分存在（6/8，规格书已有部分覆盖，需补充而非推翻），1 条含规格书自身表述修正（4，已改 §12.3）。**8 条全部成立，已落实到本规格书。**

### 13.6 三审补充（2026-08-29，经复核后的真实形态）

| # | 条目 | 复核结论 | 落定 |
|---|---|---|---|
| A | L_inv 反平凡性要求 | 问题真实，但机制已被 E4/E5/E10 覆盖；**只补"匹配强度 dropout 对照"精度 + 可证伪声明，不新增实验** | 已写入 §4.1（对照强度要求）+ §2.4（反平凡性声明） |
| B | 数据信号检验 | 问题真实且确实缺失（原仅 E13 事后分析，无事前验证；时间信号是五大贡献之一，赌其存在） | **已执行（2026-08-29）**：Amazon Health `signal-check` 结论 **SIGNAL_WEAK**——repurchase pairs=0，(user,item) max count=1（每用户每商品至多一条交互），**结构上无复购周期**（reviews 数据本质，非代码/参数问题，已对原始文件复核）；**决策：时间信号贡献（原贡献4）整体移除，健康叙事弱化为"健康类目应用域"，有效贡献收敛为 1/2/3/5** |

### 13.7 四审独立重检发现（2026-08-29，颠覆性更新）

- **Emory'26（Two-Sided SSM, Ziwen Pan）**：序列推荐中 **modality-MNAR 显式建模**（把模态缺失当信息性信号 + endogeneity 加权消息传递 + 双端时序状态演化；6 Amazon 类目 Recall@20 超 BSARec 14.8–18.8%）。**占据原贡献 1 的"首次"声明**（[Emory ETD](https://etd.library.emory.edu/concern/etds/gt54kp785)）。
- 独立核查同时确认：**缺失（missingness）× IRM × 序列 三者合一仍无任何工作**（I3-MRec 是唯一 IRM×缺失，但非序列）。
- **处理（三条，已落定）**：
  1. 立身之本移锚：MNAR 建模 → **缺失不变学习 × 健康域**（§1.3/§1.4/§1.5/§12.3 已改）；
  2. **arXiv 预印本提前至 S2 出首个鲁棒曲线后**（§7 S2/§8/§10.2 已改）；若 Emory 会议/期刊版先发，组合新颖性将进一步塌缩；
  3. 待办：核实 Emory 论文是否有会议/期刊版（影响划界写法与紧迫度）。

---

## 14. 竞赛演示设计蓝本（2026-08-29 定稿，国奖级呈现目标）

> **前端在"软件应用与开发"赛道占评分 30–40%**：五维"美"占 20%；评分表作品创意 20%（含界面设计/交互性）、功能效果 30%、技术实现 30%、**作品呈现 10%（界面美观/设计人性化/符合设定受众）**、作品文档 10%。前端不是论文可视化的副产品，而是近四成分数的独立交付物。本蓝本经评委视角自检（发现 5 个问题 → 优化），供 S6 实现。

### 14.1 国奖作品六条共性（研究来源见 §14.5）

1. **先讲故事再讲技术**（非遗木拱廊桥/中医药/矿山/教育场景，技术是故事的工具）
2. **AI 前沿交互是潮流**（LLM 语音/文本对话、拍照识别、RAG 知识库）
3. **真实落地 + 社会价值**（"解决生活中的一件小事"——与本项目哲学一致）
4. **可视化是标配**（数据分析看板、实时监测）
5. **趣味/游戏化交互**（灵宠伴学、小游戏、闯关）
6. **多模态/沉浸式加分**（AR/VR/3D/Unity WebGL，非必需是加分项）

### 14.2 三条核心设计决策

1. **"符合设定受众"是评审明文加分项** → 演示界面本身适老化设计（大字号/高对比/简单导航/可选语音朗读）。用**关怀模式 / 标准模式双开关**展示设计思考，同时保住默认界面的现代美观——化解"过度适老化显得界面简单、丢美分"的冲突。评审打开网页即信"我们为老年人做推荐"。
2. **核心 wow moment = 缺失滑块对比**：滑块 0%→90% 拖动，左侧普通系统推荐列表**肉眼可见崩坏**（命中率曲线直线下滑），右侧缺失不变系统**保持稳定**，并排实时对比。评委亲手触发变化 = 一秒钟看懂技术价值。
3. **安全门控是全场独特点（贡献 5 的演示化身，别家没有）**：低置信度案例不硬推，提示"⚠️ 证据不足，建议先咨询医生/药师"。适老化健康场景下 = 技术深度 + 社会责任 + 记忆点三合一。

### 14.3 演示结构（品牌化 6 模块 / 5 屏）

系统命名 + LOGO + 统一视觉（暖色橘/青、圆角、留白、呼吸感；工作名"暖荐"可改）。屏：

| 屏 | 内容 | 作用 |
|---|---|---|
| ① 开场场景 | **王奶奶半年购买时间轴**：健康类目/用药相关商品高亮 + 点击查看当时缺失模态（图缺/文缺） | 故事与技术第一次咬合；10 秒进入情境 |
| ② 核心对比实验台 | 缺失滑块 + 双系统并排 + 实时命中率曲线 + **"技术原理"动画开关**（缺失环境构造→表示对齐→免疫） | wow moment；把技术难度从后台搬到前台 |
| ③ 推荐解释 + 安全门控 | 点商品 → 置信度/缺失模态/推荐理由（类目/历史/缺失模态）；低置信 → "建议咨询医生/药师" | 技术深度 + 可解释 + 独特点 |
| ④ 关怀模式切换 | 标准/关怀双模式（字号/对比/布局/语音朗读） | 符合设定受众的可见设计决策 |
| ⑤ 数据总览 | 三数据集缺失率 vs Recall/NDCG **retention 曲线**（E2/E4 真实实验输出） | 工作量 + 论文图联动 |

技术栈：**Vue3/React + ECharts + FastAPI 后端调用真实训练模型**（不 mock 数据——"技术实现 30%"要看到真实推理链路）。

### 14.4 自检评分与诚实边界

**五维自检（优化前 → 优化后）**：创 6.5→8.5（缺失滑块+安全门控+关怀模式组合无同类）、量 5→8（6 模块+真实数据+后端推理+原理动画）、健 7→8.5（真实模型后端+稳定交互）、技 6→8（原理可视化+真实推理链路+前端工程规范）、美 7→8.5（品牌识别+双模式+暖色设计系统）。**呈现层面（创意/功能/呈现 ≈ 60% 权重）达到国奖水平。**

**诚实边界（避免把前端当救世主）**：
1. 前端仅占 30–40%——国一取决于"技术真行 + 结果强 + 文档好 + 呈现美"四者共同决定；**若 E2 实验打不过基线，前端救不回（省一仍稳）**；
2. **演示必须用真实模型输出（训练出的 retention 曲线），禁止 mock/编数字**——与科研诚信红线一致；
3. 演示是 S6 的事，S1 主线在前，**Go/No-Go 通过后再动手**；本蓝本只是提前定设计；
4. 提交需配套 **5 分钟演示视频剪辑 + 一页纸说明文档**（作品文档 10%），可提前规划。

### 14.5 资料来源（国奖作品与评审标准）

- 评审标准：第 18 届软件应用与开发权重（创意20/功能30/技术30/呈现10/文档10）+ 五维评选（创/量/健/技/美）
- 国奖作品：第 19 届《虹桥寻梦》（4K+3D+AI 木拱廊桥）、湖南中医药《百草千方》、贵州中医药《中药科普 AI 馆》、第 17 届《通慧智教》（Vue3+Unity WebGL 智能教学网站，[开源 GitHub](https://github.com/TochusC/ai-assistant-teaching-website)）、《第二课堂智能数据分析平台》（文心大模型）、诗韵时光机（AI+AR+VR）
- 演示视频合集：[B 站历年获奖作品](https://www.snm0516.aisee.tv/video/BV1d6dYB8EDD/)、[国一演示视频示例](https://www.bilibili.com/video/BV15MvLejEt5/)

> 注：国奖作品的具体 UI 细节大多不公开，能直接学习的是评审标准 + 作品特征描述 + 公开演示视频；最终 UI 需自行设计与打磨。
